# express-sequelize-crud-generator
Simple CRUD generator for express API with sequelize as ORM

# Usage
1. Clone to the main project derectory
2. Run `node ./express-sequelize-crud-generator/index.js` from main project directory
